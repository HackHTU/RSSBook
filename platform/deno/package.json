{
	"dependencies": {
		"rssbook": "workspace:*"
	},
	"name": "@rssbook/platform-deno",
	"private": true,
	"scripts": {
		"build": "deno check --config deno.json --lock deno.lock index.ts",
		"dev": "deno run --config deno.json --lock deno.lock --allow-net --allow-env --allow-read --watch index.ts",
		"start": "deno run --config deno.json --lock deno.lock --allow-net --allow-env --allow-read index.ts"
	},
	"type": "module"
}
